#include<bits/stdc++.h>

using namespace std;

int longestCommonSubusequence(string s1, string s2) {
    
}

int main() {
    string s1, s2;
    getline(cin, s1);
    getline(cin, s2);
    int lcs = longestCommonSubusequence(s1, s2);
    cout << lcs << endl;
    return 0;
}